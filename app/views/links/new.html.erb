<%= form_for @link, url: { action: @action } do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :url %>
  <%= f.url_field :url, required: true %>
  <br>
  <%= f.submit %>
<% end %>

<% if flash[:alert] %>
  <%= flash.alert %>
<% end %>
